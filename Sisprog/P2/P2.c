#include <stdio.h>

typedef struct Estado {
    
    int num;
    /* data */
};


int main(){

    struct Estado Eat, Einit;

    switch (Eat.num){

        case (Eat == Einit):
            /* code */
            break;
    
        default:
            break;
    }

    return 0;
}